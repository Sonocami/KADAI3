<!DOCTYPE html>
<html class="fontawesome-i2svg-active fontawesome-i2svg-complete">
  <head>
    <title>Bookers2V2</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>


    <%= stylesheet_pack_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
  </head>





  <body>
    <header>
    <!--if user~はdevise側のヘルパーメソッド-->
     <nav class="navbar nav bar-expand-lgnavbar-dark bg-dark">
      <div class="container">
          <%=link_to "Bookers",root_path, class:'navbar-brand'%>

    <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
     <span class="navbar-toggler-icon"></span>
     </button>

    <div class="navbar-collaps collapse show" id="navbarNavDropdown"style>
    <ul class="navbar-nav ml-auto">

    <% if user_signed_in? %>
      <li class="nav-item">
        <%= link_to user_path(current_user), class:"navi-link text-light" do%>
        <span><i class="user_path"></i>Home</span>
        <%end%>
      </li>

      <li class="nav-item">
        <%=link_to users_path, class:"navi-link text-light" do%>
      <span><i class="fas fa-users"></i>Users</span>
      <%end%>
      </li>

      <li class="nav-item">
        <%=link_to books_path, class:"navi-link text-light" do%>
      <span><i class="fas fa-book-open"></i>Books</span>
      <%end%>
      </li>

      <li class="nav-item">
        <%= link_to destroy_user_session_path, method: :delete, class:"navi-link text-light" do%>
      <span><i class="fas fa-sign-out-alt"></i>Log out</span>
      <%end%>
      </li>

      <% else %>
       <li>
        <%=link_to root_path,class:"navi-link text-light" do%>
        <span><i class="fas fa-home"></i>Home</span>
        <% end %>
       </li>

       <li>
        <%=link_to home_about_path,class:"navi-link text-light" do%>
       <span><i class="fas fa-link"></i>About</span>
       <%end%>
       </li>

       <li>
        <%=link_to new_user_registration_path,class:"navi-link text-light" do%>
        <span><i class="fas fa-user-plus"></i>Sign up</span>
        <%end%>
        </li>

        <li>
        <%=link_to new_user_session_path,class:"navi-link text-light" do%>
        <span><i class="fas fa-sign-in"></i>Log in</span>
        <%end%>
        </li>
      <% end %>

    </ul>
   </div>
   </div>
  </nav>
 <%= yield %>

 </header>
</body>



  <footer>
    <div class=“container mt-5”>
      <div class=”row”>
       <div class=“mx-auto”>
        <p>CopyRight Infratop.inc</p>
       </div>
      </div>
    </div>
  </footer>
</html>
