<%=render'users/user',user:@user%> <!-- 部分テンプレート-->

<div class="row">
<div class="col-md-8 offset-md-1">
 <h2>Books</h2>
  <table class="table table-hovertable-inverse">

   <thead>
     <tr>
      <th></th>
      <th>title</th>
      <th>opinion</th>
      </tr>
    </thead>

     <% @books.each do |books|%>
    <tbody>
    <tr>
     <td><%=image_tag books.user.get_profile_image(100,100)%></td>
     <td><%=link_to 'title',book_path(books.id)%></td>
     <td><%=books.body%></td>
    </tr>
    <%end%>
    </tbody>
   </table>
</div>
</div>
</main>